<UserControl
    x:Class="DustInTheWind.ClockWpf.TemplateEditor.Presentation.Shapes.ShapesControl"
    
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:shapes="clr-namespace:DustInTheWind.ClockWpf.TemplateEditor.Presentation.Shapes"
    
    mc:Ignorable="d"
    d:DesignHeight="450"
    d:DesignWidth="800"
    d:DataContext="{d:DesignInstance shapes:ShapesViewModel}">

    <Grid
        Grid.Column="2">

        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="300"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <TextBlock
            Grid.Row="0"
            Text="!!! Work in progress"
            TextWrapping="Wrap"
            FontStyle="Italic"
            Foreground="#666666"
            Margin="10 8 10 0" />

        <ListBox
            Grid.Row="1"
            Margin="0 10 0 0"
            ItemsSource="{Binding Shapes, Mode=OneWay}"
            SelectedItem="{Binding SelectedShape}"
            DisplayMemberPath="">

            <ListBox.ItemTemplate>
                <DataTemplate>
                    <Border Padding="0 1">
                        <StackPanel>

                            <TextBlock
                                Text="{Binding Name, Mode=OneWay}"
                                FontWeight="DemiBold"/>

                            <TextBlock
                                Text="{Binding Type, Mode=OneWay}"
                                FontSize="11"
                                Foreground="Gray"
                                Margin="10 0 0 0"/>

                        </StackPanel>
                    </Border>
                </DataTemplate>
            </ListBox.ItemTemplate>

        </ListBox>

        <GridSplitter
            Grid.Row="2"
            HorizontalAlignment="Stretch"
            VerticalAlignment="Stretch"
            Height="10">

            <GridSplitter.Template>
                <ControlTemplate TargetType="{x:Type GridSplitter}">
                    <Grid Background="Transparent">

                        <Ellipse
                            Width="55"
                            Height="2"
                            Fill="Gray"
                            HorizontalAlignment="Center"
                            VerticalAlignment="Center" />

                    </Grid>
                </ControlTemplate>
            </GridSplitter.Template>

        </GridSplitter>

        <Control/>

    </Grid>

</UserControl>
