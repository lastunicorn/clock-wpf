<UserControl
    x:Class="DustInTheWind.ClockWpf.TemplateEditor.Presentation.CabinetArea.CabinetControl"
    
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:clockWpf="clr-namespace:DustInTheWind.ClockWpf;assembly=DustInTheWind.ClockWpf"
    xmlns:local="clr-namespace:DustInTheWind.ClockWpf.TemplateEditor.Presentation.CabinetArea"
    
    mc:Ignorable="d"
    d:DesignHeight="450"
    d:DesignWidth="800"
    d:DataContext="{d:DesignInstance local:CabinetViewModel}"
    Background="#fcfcfc">

    <ScrollViewer>
        <ItemsControl
            ItemsSource="{Binding ClockTemplates}"
            ScrollViewer.VerticalScrollBarVisibility="Auto">

            <ItemsControl.ItemsPanel>
                <ItemsPanelTemplate>
                    <WrapPanel IsItemsHost="True" />
                </ItemsPanelTemplate>
            </ItemsControl.ItemsPanel>

            <ItemsControl.ItemTemplate>
                <DataTemplate>
                    <DockPanel
                        Margin="15">

                        <TextBlock
                            DockPanel.Dock="Bottom"
                            Text="{Binding Name}"
                            HorizontalAlignment="Center"
                            Margin="0 10 0 0"
                            FontSize="14"/>

                        <clockWpf:AnalogClock
                            ClockTemplate="{Binding ClockTemplate}"
                            Width="200"
                            Movement="{Binding DataContext.Movement, RelativeSource={RelativeSource AncestorType=Window}}"/>

                    </DockPanel>
                </DataTemplate>
            </ItemsControl.ItemTemplate>

        </ItemsControl>

    </ScrollViewer>

</UserControl>
