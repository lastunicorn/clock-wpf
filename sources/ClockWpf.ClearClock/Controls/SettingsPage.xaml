<UserControl
    x:Class="DustInTheWind.ClockWpf.ClearClock.Controls.SettingsPage"
    
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:controls="clr-namespace:DustInTheWind.ClockWpf.ClearClock.Controls"
    xmlns:templates="clr-namespace:DustInTheWind.ClockWpf.ClearClock.Controls.Templates"
    xmlns:about="clr-namespace:DustInTheWind.ClockWpf.ClearClock.Controls.About"
    
    mc:Ignorable="d"
    d:DesignHeight="400"
    d:DesignWidth="400"
    d:DataContext="{d:DesignInstance controls:SettingsPageModel}">

    <Border
        Background="WhiteSmoke">

        <Grid>

            <Grid.RowDefinitions>
                <RowDefinition Height="1*"/>
                <RowDefinition Height="4*"/>
                <RowDefinition Height="1*"/>
            </Grid.RowDefinitions>

            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="1*" />
                <ColumnDefinition Width="6*" />
                <ColumnDefinition Width="1*" />
            </Grid.ColumnDefinitions>

            <Border
                Grid.Row="0"
                Grid.ColumnSpan="3"
                Background="LightGray"
                BorderBrush="Gray"
                BorderThickness="0 0 0 1">

                <TextBlock
                    Text="Clear Clock"
                    FontSize="20"
                    FontWeight="Bold"
                    VerticalAlignment="Center"
                    HorizontalAlignment="Center"
                    Visibility="Visible"/>

            </Border>

            <Border
                Padding="8"
                Grid.Row="1"
                Grid.Column="1">

                <TabControl
                    BorderThickness="0"
                    Background="WhiteSmoke"
                    Padding="0 10 0 10">

                    <TabControl.Resources>
                        <Style TargetType="TabItem">
                            <Setter Property="Template">
                                <Setter.Value>
                                    <ControlTemplate TargetType="TabItem">
                                        <Grid Name="Panel">
                                            <ContentPresenter
                                                x:Name="ContentSite"
                                                VerticalAlignment="Center"
                                                HorizontalAlignment="Center"
                                                ContentSource="Header"
                                                Margin="10,2"/>
                                        </Grid>
                                        <ControlTemplate.Triggers>
                                            <Trigger Property="IsSelected" Value="True">
                                                <Setter TargetName="Panel" Property="Background" Value="LightGray" />
                                            </Trigger>
                                        </ControlTemplate.Triggers>
                                    </ControlTemplate>
                                </Setter.Value>
                            </Setter>
                        </Style>
                    </TabControl.Resources>

                    <TabItem
                        Header="Templates"
                        Background="WhiteSmoke"
                        BorderThickness="0">

                        <templates:TemplatesControl
                            DataContext="{Binding TemplatesViewModel}"/>

                    </TabItem>

                    <TabItem
                        Header="About"
                        Background="WhiteSmoke">

                        <about:AboutControl
                            DataContext="{Binding AboutViewModel}"/>

                    </TabItem>

                </TabControl>

            </Border>

            <Button
                Grid.Row="2"
                Grid.ColumnSpan="3"
                Content="Close"
                FontSize="20"
                Background="LightGray"
                Command="{Binding SettingsCloseCommand}"
                BorderBrush="Gray"
                BorderThickness="0 1 0 0"/>

        </Grid>

    </Border>

</UserControl>
