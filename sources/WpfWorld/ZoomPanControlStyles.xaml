<ResourceDictionary
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="clr-namespace:DustInTheWind.WpfWorld"
    xmlns:behaviors="clr-namespace:DustInTheWind.WpfWorld.Behaviors"
    xmlns:converters="clr-namespace:DustInTheWind.WpfWorld.Converters">

    <converters:PointToStringConverter x:Key="PointToStringConverter" />

    <Style TargetType="{x:Type local:ZoomPanControl}">
        <Setter Property="Background" Value="Transparent" />
        <Setter Property="HorizontalContentAlignment" Value="Center" />
        <Setter Property="VerticalContentAlignment" Value="Center" />

        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="{x:Type local:ZoomPanControl}">

                    <Grid
                        x:Name="PART_Container"
                        Background="{TemplateBinding Background}"
                        Cursor="Hand"
                        ClipToBounds="True">

                        <ContentPresenter
                            x:Name="PART_Content"
                            Content="{TemplateBinding Content}"
                            ContentTemplate="{TemplateBinding ContentTemplate}"
                            HorizontalAlignment="{TemplateBinding HorizontalContentAlignment}"
                            VerticalAlignment="{TemplateBinding VerticalContentAlignment}"/>

                        <StackPanel
                            Orientation="Horizontal"
                            FlowDirection="RightToLeft"
                            HorizontalAlignment="Right"
                            VerticalAlignment="Top">
                            
                            <Button
                                Command="{TemplateBinding ResetViewCommand}"
                                CommandParameter="{Binding ElementName=PART_Content}"
                                Cursor="Arrow"
                                ToolTip="Reset zoom and position"
                                BorderThickness="0"
                                Width="35"
                                Height="35">

                                <TextBlock
                                    Text="⟲"
                                    FontSize="20"/>

                            </Button>

                            <Slider
                                Width="200"
                                VerticalAlignment="Center"
                                Margin="0 0 10 0"
                                Minimum="0.1"
                                Maximum="5.0"
                                TickFrequency ="0.1"
                                Value="{Binding ZoomValue, RelativeSource={RelativeSource TemplatedParent}, Mode=TwoWay}"
                                IsSnapToTickEnabled="True"
                                FlowDirection="LeftToRight"/>

                        </StackPanel>

                        <TextBlock
                            VerticalAlignment="Bottom"
                            HorizontalAlignment="Right"
                            Text="{Binding Location, RelativeSource={RelativeSource TemplatedParent}, Converter={StaticResource PointToStringConverter}}" />

                    </Grid>

                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

</ResourceDictionary>